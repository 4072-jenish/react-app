  const initialState = {
      products: [],
      product : null,
      isLoading: false
  }

const ProductReducer = (state = initialState, action) => {
    switch (action.type) {
         default: return state;
    }
}

export default ProductReducer;
